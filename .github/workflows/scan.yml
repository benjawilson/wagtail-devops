name: Code Quality and Security Scans

on: push

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:

    - name: Pylint scan
      run: |
        python -m pip install --upgrade pip
        pip install pylint
        pylint **/*.py